{
  "name": "chatgpt-context-guard",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/core.test.js tests/dom.test.js tests/conversation.test.js tests/extension.test.js",
    "test:browser": "node --test tests/browser.test.js",
    "lint": "node scripts/check.js",
    "build": "node scripts/build.js",
    "verify": "npm test && npm run lint && npm run build && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}